spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        prefix: nacos-provider
        file-extension: yaml
  profiles:
    active: dev